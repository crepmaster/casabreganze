# Clé API Anthropic (https://console.anthropic.com) — requise sauf en --dry-run
ANTHROPIC_API_KEY=sk-ant-...

# Optionnel : dossier de sortie des guides (défaut = ../easyrest-site/src/content/guides)
# EASYREST_GUIDES_DIR=/chemin/vers/src/content/guides

# Optionnel : score qualité minimal pour publier (sinon draft: true). Défaut 70
# EASYREST_MIN_QUALITY=70
